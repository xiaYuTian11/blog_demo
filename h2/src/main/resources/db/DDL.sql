create table if not exists USER (
                                    USE_ID int not null primary key,
                                    USE_NAME varchar(100),
                                    USE_SEX varchar(1),
                                    USE_AGE int);